---
const { title = "Portfolio" } = Astro.props;
---

<!DOCTYPE html>
<html lang="it">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{title}</title>

    <!-- FONT MANGA MODERNO -->
    <link href="https://fonts.googleapis.com/css2?family=Noto+Sans+JP:wght@300;500;700&display=swap" rel="stylesheet">

    <style>
      :root {
        --bg: #f8f8f8;      /* bianco carta leggero */
        --ink: #111111;     /* nero inchiostro */
        --gray: #e5e5e5;    /* grigio chiaro */
        --gray-dark: #777;
      }

      * {
        box-sizing: border-box;
      }

      body {
        font-family: "Noto Sans JP", sans-serif;
        font-size: 17px;
        margin: 0;
        padding: 0;
        background: var(--bg);
        color: var(--ink);
        line-height: 1.6;
      }

      /* Header stile manga moderno */
      header {
        background: var(--ink);
		color: yellow;
        padding: 14px 24px;
        border-bottom: 6px solid var(--gray); /* bordo spesso stile fumetto */
      }

      header nav {
        max-width: 1600px;
        margin: 0 auto;
        display: flex;
        align-items: center;
        gap: 30px;
      }

      header nav a {
        color: white;
        text-decoration: none;
        font-weight: 700;
        font-size: 18px;
        letter-spacing: 0.5px;
        transition: opacity 0.2s;
      }

      header nav a:hover {
        opacity: 0.7;
      }

      /* Contenuto */
  


main {
  background: red;
}

 
      /* Footer manga moderno */
      footer {
        text-align: center;
        padding: 30px;
        color: var(--gray-dark);
        border-top: 6px solid var(--ink);
        margin-top: 50px;
      }
    </style>
  </head>

  <body>

<header>
  <nav>
    <a href="/">Home</a>
    <a href="/fumetti">Fumetti</a>
  </nav>
</header


    <main>
      <slot />
    </main>

    <footer>
      © {new Date().getFullYear()} Luciano Mariani – Portfolio Manga
    </footer>
  </body>
</html>